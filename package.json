{
  "name": "gossamer-workspace",
  "private": true,
  "scripts": {
    "build": "npm --prefix web run build",
    "test:barrier": "node web/scripts/test-information-barrier.mjs",
    "test:clean-room": "npm --prefix web run test:clean-room",
    "test:contracts": "npm --prefix web run test:contracts && npm run test:clean-room"
  }
}
